# As Fasttext can only run on Mac, the code is on the other remove computer.

# import fasttext
# def train_model(pattern):
#     classifier = fasttext.supervised(pattern +'train.txt', 'model')
#     result = classifier.test(pattern +'test.txt')